/**
 * for( variable; condicion; incremento++ ) {
 *
 *      // Codigo que se ejecuta dentro del for
 *
 * }
 */

let list = ["eat", "sleep", "code", "repeat"];

for( i = 0; i < list.length ; i++ ){
    console.log( list[i] );
}