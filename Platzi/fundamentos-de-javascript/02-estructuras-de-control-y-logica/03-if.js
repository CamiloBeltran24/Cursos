/*
    if ( let === algo) {
        // Codigo
    } else if { // SI NO - SI
        // Codigo
    } else {

    }
*/

let nombre = "PEPE";

if ( nombre === "Christian" ) {
    console.log(`Hola ${nombre}`);
} else if( nombre === "Nico" ) {
    console.log(`Hola ${nombre}`);
} else {
    console.log(`Nombre ${nombre} no encontrado`);
}